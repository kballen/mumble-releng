diff -Naur glib-2.42.2-old/configure glib-2.42.2-new/configure
--- glib-2.42.2-old/configure	2015-02-25 19:09:05.000000000 -0800
+++ glib-2.42.2-new/configure	2015-07-12 18:51:15.464352947 -0700
@@ -26877,7 +26877,9 @@
 }
 _ACEOF
 if ac_fn_c_try_link "$LINENO"; then :
-  as_fn_error $? "GLib must be build with -march=i486 or later." "$LINENO" 5
+  { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: GLib must be build with -march=i486 or later." >&5
+$as_echo "$as_me: WARNING: GLib must be build with -march=i486 or later." >&2;}
+                       SAVE_CFLAGS="${SAVE_CFLAGS} -march=i486"
 fi
 rm -f core conftest.err conftest.$ac_objext \
     conftest$ac_exeext conftest.$ac_ext
